This bot is used to spread ideas of TBCITW throughout VK*
*This organisation is recognised as the most trashy organization outside of Russia

TODO:
- [x] LLM answers and have a context of some part of the dialogue
- [x] Send friend requests
- [x] Cache friends and friend requests to the memory
- [x] Create events from unread to answer on messages during the bot's inactivity
- [x] Use a DB (Redis) to store information about the progress state of each user
- [x] Rewrite configs in such a way that all can be set up from environment variables, so creating multiple userbots will be as easy as getting a flood error from VK API
- [ ] Use a captcha solver to prevent VK's attempts to evade TBCITW's spreading with CAPTCHA errors
- [ ] Auto write to friends who applied a friend request
